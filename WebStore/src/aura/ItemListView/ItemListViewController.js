/**
 * Created by venzor on 3/23/2019.
 */
({
    doInit: function (cmp, evt, hlp) {
        hlp.initList(cmp);
    },

    reloadComponent : function (cmp, evt, hlp) {
        hlp.reloadList(cmp)
    }

})